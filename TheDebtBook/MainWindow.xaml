<Window x:Class="TheDebtBook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TheDebtBook"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=MainViewModel}"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="Depters" Margin="10,10" Grid.Row="0"></TextBlock>
        <DataGrid Grid.Row="1" Name="dgrdDepters" AutoGenerateColumns="False"  ItemsSource="{Binding Path=Debtors}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Debt" Binding="{Binding Path=Debt}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Column="1" Grid.Row="0" Name="btnAdd" Content="Add"></Button>

        <TextBox Text="Name" Grid.Column="1" Grid.Row="1"></TextBox>
        <TextBox Text="Dept" Grid.Column="1" Grid.Row="2"></TextBox>

        <TextBox Grid.Column="1" Grid.ColumnSpan="2" Margin="27,37,-72.333,0.333" Grid.RowSpan="2" />
        <TextBox Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Margin="27,45.667,-72.333,-0.333" Grid.RowSpan="2" ></TextBox>
    </Grid>
</Window>
